name: Create Release

on: [ push ]

jobs:
  build-from-source:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: list files after checkout
        run: |
          pwd
          ls -laR
          echo "================================"
          find .

