name: Create Release

on: [ push ]

jobs:
  build-from-source:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: run build shell script
        run: ./build_from_source.sh

